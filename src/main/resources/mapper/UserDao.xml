<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qsd.bookstore.dao.UserDao">

	<select id="login" resultType="user" parameterType="com.qsd.bookstore.dto.UserByLogin">
		select * 
		from user 
		where username = #{username} and password = #{password}
	</select>
	
	<insert id="register" parameterType="user">
		insert into user(username, password, nickname, gender, age)
		values(#{username}, #{password}, #{nickname}, #{gender}, #{age})
	</insert>

</mapper>
